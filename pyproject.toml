[project]
name = "sql-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.18.0",
    "pydantic-settings>=2.2.1",
]

[project.scripts]
sql-mcp-server = "sql_mcp_server:main"

[tool.uv]
package = true
